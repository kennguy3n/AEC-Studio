name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    # Defaults (opened, synchronize, reopened) plus the label events.
    # Adding `test-all-platforms` to an existing PR re-fires the
    # workflow with that label visible in `github.event.pull_request.labels`,
    # so the gate job can flip into the full matrix without the
    # contributor having to push an empty commit or click "Re-run".
    # `unlabeled` is included for symmetry — removing the label
    # snaps the next run back to the Ubuntu-only baseline.
    types: [opened, synchronize, reopened, labeled, unlabeled]

# Cancel in-progress PR runs when a new commit / label change
# supersedes them, so a contributor pushing a fixup commit (or
# adding the `test-all-platforms` label after a `synchronize` run
# already started) doesn't burn two parallel runner sets on the
# same PR. Push events on `main` are intentionally NOT cancelled
# — every commit on `main` must produce a CI result for the
# `package-*` gate and release traceability.
#
# `cancel-in-progress` is further gated on the *kind* of new event:
# only `synchronize` / `opened` / `reopened` (the default PR
# actions) and a `labeled`/`unlabeled` on the scope-changing
# `test-all-platforms` label may supersede an in-progress run.
# An unrelated label triage event (e.g. adding `needs-review`)
# must NOT cancel a legitimate run — the gate job's own `if:`
# predicate skips the replacement run for unrelated labels, so an
# unconditional cancel would leave the PR with zero CI results
# until the next push.
#
# The concurrency `group` key intentionally uses `github.sha` on
# push events and `github.ref` on PR events. PR events still
# share a per-branch group so a `synchronize` run supersedes the
# previous one for the same PR. Push events, on the other hand,
# need a per-SHA group: GitHub Actions cancels any *pending*
# (queued) run in the same group when a third run arrives, even
# with `cancel-in-progress: false` (`false` only protects the
# *running* workflow). Without per-SHA isolation, a burst of
# rapid merges to `main` (or a merge queue) would drop the CI
# results for intermediate commits, violating the
# "every commit on `main` must produce a CI result for the
# `package-*` gate and release traceability" invariant.
concurrency:
  group: >-
    ci-${{ github.workflow }}-${{
      github.event_name == 'push' && github.sha || github.ref
    }}
  cancel-in-progress: >-
    ${{ github.event_name == 'pull_request'
        && (
          (github.event.action != 'labeled' && github.event.action != 'unlabeled')
          || github.event.label.name == 'test-all-platforms'
        ) }}

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: "-D warnings"

jobs:
  # ---------------------------------------------------------------
  # gate: compute which OS matrix the rust/typescript jobs should
  # run on for this trigger.
  #
  # Defaults:
  #   * push to main           → full matrix (ubuntu + macos + windows)
  #   * PR with label
  #     `test-all-platforms`   → full matrix
  #   * any other PR           → ubuntu only
  #
  # Rationale: platform-specific CI (especially the Electron-install
  # path on macOS-arm64 and Windows) is occasionally flaky for
  # reasons unrelated to the PR's diff (npm registry / electron CDN
  # 404s, runner image regressions, etc.). For PRs that don't touch
  # platform-specific code (Rust-only, docs-only, etc.) we run a
  # fast and stable baseline — Ubuntu Rust + Ubuntu TypeScript —
  # and gate the full cross-platform sweep on the merge to main,
  # where any real platform regression is caught before it can
  # affect a release.
  #
  # To opt a PR into the full matrix (e.g. when you intentionally
  # touch Electron / packaging / OS-specific Rust code), add the
  # `test-all-platforms` label to the PR.
  # ---------------------------------------------------------------
  gate:
    name: Gate (compute OS matrix)
    runs-on: ubuntu-latest
    # Skip the entire workflow when the trigger is a `labeled` /
    # `unlabeled` event on a label that doesn't change CI scope.
    # `test-all-platforms` flips the matrix; any other label
    # (e.g. `needs-review`, `priority-high`) must not burn runner
    # minutes (the concurrency block above also prevents the
    # replacement run from cancelling an in-progress legitimate
    # run for the same unrelated label event). The first conjunct
    # keeps the default `opened`, `synchronize`, `reopened`, and
    # all `push` events flowing. Explicit parens make the
    # operator precedence (`&&` binds tighter than `||`) visible
    # without having to remember the GitHub Actions expression
    # grammar.
    if: >-
      (github.event.action != 'labeled' && github.event.action != 'unlabeled')
      || github.event.label.name == 'test-all-platforms'
    outputs:
      os_matrix: ${{ steps.matrix.outputs.os_matrix }}
    steps:
      - id: matrix
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_LABELS: ${{ toJSON(github.event.pull_request.labels.*.name) }}
        run: |
          set -euo pipefail
          full='["ubuntu-latest","macos-latest","windows-latest"]'
          stable='["ubuntu-latest"]'
          if [ "$EVENT_NAME" = "push" ]; then
            os="$full"
            mode="FULL (push to main)"
          # `$PR_LABELS` is the JSON-encoded array of label names
          # (e.g. `["test-all-platforms","needs-review"]`), so we
          # match the *quoted* form to avoid substring false
          # positives like `no-test-all-platforms-please`.
          elif echo "$PR_LABELS" | grep -qF '"test-all-platforms"'; then
            os="$full"
            mode="FULL (test-all-platforms label)"
          else
            os="$stable"
            mode="STABLE (Ubuntu-only baseline)"
          fi
          echo "Gate decision: $mode"
          echo "os_matrix=$os"
          echo "os_matrix=$os" >> "$GITHUB_OUTPUT"
          # Also surface the decision in the job summary so the run
          # is self-documenting in the GitHub UI without having to
          # drill into step logs.
          {
            echo "### CI gate decision"
            echo
            echo "- Event: \`$EVENT_NAME\`"
            echo "- Mode: **$mode**"
            echo "- OS matrix: \`$os\`"
          } >> "$GITHUB_STEP_SUMMARY"

  # ---------------------------------------------------------------
  # no-python-runtime: enforce the "no Python in the shipped runtime"
  # invariant on every PR.
  #
  # Phase 9 removed the Blender and IFC Python workers, and Phase 18
  # eliminated the last lingering Python AI-runtime artifacts (the
  # workers/ai/ config.json and the MLXAdapter doc-fiction). The two
  # remaining .py files (scripts/generate_template_previews.py and
  # packaging/generate_icons.py) are **build-time tooling** that runs
  # on the CI runner to emit PNGs; they are not invoked at app runtime
  # and are not bundled in any installer. They are pinned in the
  # allow-list below.
  #
  # This job is fast (~5 s) and runs unconditionally so a contributor
  # accidentally reintroducing a Python dependency or a Python runtime
  # worker gets a clear, immediate signal at the PR stage rather than
  # only after `package-*` runs on `main`.
  # ---------------------------------------------------------------
  no-python-runtime:
    name: No Python in runtime
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Reject new Python runtime artifacts
        run: |
          set -euo pipefail
          fail=0

          # Allow-list of build-time .py scripts. Any other .py file
          # added under the repo (outside .git / node_modules / target)
          # fails the job.
          allowed='^\(scripts/generate_template_previews\.py\|packaging/generate_icons\.py\)$'
          unexpected_py=$(
            find . \
              -path ./node_modules -prune -o \
              -path ./target -prune -o \
              -path ./.git -prune -o \
              -name '*.py' -print 2>/dev/null \
              | sed 's|^\./||' \
              | grep -v "$allowed" || true
          )
          if [ -n "$unexpected_py" ]; then
            echo '::error::Unexpected Python files in the repo:'
            echo "$unexpected_py"
            echo
            echo 'Only build-time tooling .py files are allowed; the shipped runtime must contain no Python. See docs/AI_RUNTIME.md for the policy. To add a new build-time .py, update the allow-list in .github/workflows/ci.yml::no-python-runtime.'
            fail=1
          fi

          # Reject every Python dependency manifest. The shipped
          # runtime must not pull a Python interpreter.
          dep_files=$(
            find . \
              -path ./node_modules -prune -o \
              -path ./target -prune -o \
              -path ./.git -prune -o \
              \( -name 'requirements*.txt' \
                 -o -name 'pyproject.toml' \
                 -o -name 'Pipfile' \
                 -o -name 'Pipfile.lock' \
                 -o -name 'poetry.lock' \
                 -o -name 'uv.lock' \
                 -o -name 'environment.yml' \
                 -o -name 'conda*.yaml' \
                 -o -name 'setup.py' \
                 -o -name 'setup.cfg' \
              \) -print 2>/dev/null || true
          )
          if [ -n "$dep_files" ]; then
            echo '::error::Python dependency manifests are not allowed:'
            echo "$dep_files"
            fail=1
          fi

          # Reject workers/ai|blender|ifc directories. Phase 9 + 18
          # eliminated all three.
          for d in workers/ai workers/blender workers/ifc; do
            if [ -d "$d" ]; then
              echo "::error::Removed-in-prior-phase directory has been re-added: $d"
              fail=1
            fi
          done

          # Reject MLXAdapter / mlx-lm / gemlite references *in source*
          # (docs that document the no-Python decision are allowed; the
          # grep below excludes .md files). gemlite / HQQ are Python
          # quantization runtimes for the bonsai-image-* models that
          # cannot ship under the no-Python constraint.
          bad_imports=$(
            grep -RIn --include='*.rs' --include='*.ts' --include='*.tsx' \
              --include='*.js' --include='*.jsx' \
              -e 'MLXAdapter' -e 'mlx-lm' -e '"gemlite"' -e "'gemlite'" \
              -e '"HQQ"' -e "'HQQ'" \
              . 2>/dev/null \
              | grep -v '^./node_modules' \
              | grep -v '^./target' \
              || true
          )
          if [ -n "$bad_imports" ]; then
            echo '::error::Python-only AI runtime references found in source:'
            echo "$bad_imports"
            fail=1
          fi

          # Reject electron-builder configs that bundle Python or MLX.
          for cfg in packaging/macos/electron-builder.macos.yml \
                     packaging/windows/electron-builder.windows.yml \
                     packaging/linux/electron-builder.linux.yml; do
            if [ -f "$cfg" ]; then
              hits=$(grep -in -E 'python|mlx|gemlite|conda' "$cfg" || true)
              if [ -n "$hits" ]; then
                echo "::error::Forbidden Python/MLX references in $cfg:"
                echo "$hits"
                fail=1
              fi
            fi
          done

          if [ "$fail" -ne 0 ]; then
            exit 1
          fi
          echo 'OK — no Python runtime artifacts found.'

  rust:
    name: Rust (${{ matrix.os }})
    needs: gate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(needs.gate.outputs.os_matrix) }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry & target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

      - name: cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: cargo test
        run: cargo test --all --no-fail-fast

  libredwg_oracle:
    # Cross-check files written by aec_cad's DwgWriter against
    # LibreDWG's upstream `dwgread` and `dwg2dxf` for every supported
    # AutoCAD version (R12 → R2018). LibreDWG is the de-facto open
    # reference reader; if it opens our files without error and the
    # entity inventory matches what we wrote, we have strong evidence
    # the bit-level encoding is compliant.
    #
    # **Blocking gate.** As of PR-G this job is part of the required
    # checks set. All eight versions must:
    #   1. exit 0 from `dwgread`
    #   2. emit no `Error:` or `ERROR` lines on stderr
    #   3. emit no `Warning:` lines EXCEPT a small allow-list of
    #      known LibreDWG-decoder bugs that fire on its own bundled
    #      fixtures and cannot be silenced from the writer side, plus
    #      the spec-correct HANDSEED's intentional dangling-handle
    #      warning (see Allow-list in the gate step below).
    #   4. their DXF round-trip must contain LINE / CIRCLE / TEXT
    #      entities (except R12 which is the only remaining
    #      empty-document fixture).
    #
    # LibreDWG isn't packaged in Ubuntu, so we build it from source.
    # The build is cached so the steady-state CI run only takes the
    # time to actually invoke `dwgread` on each fixture.
    name: LibreDWG oracle (R12 → R2018)
    needs: gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build deps for LibreDWG
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            autoconf automake libtool pkg-config swig \
            libpcre2-dev texinfo

      - name: Cache LibreDWG build
        id: libredwg_cache
        uses: actions/cache@v4
        with:
          path: |
            ~/libredwg-install
          key: libredwg-0.13.3-${{ runner.os }}

      - name: Build LibreDWG from source
        if: steps.libredwg_cache.outputs.cache-hit != 'true'
        run: |
          set -e
          cd /tmp
          curl -L https://ftp.gnu.org/gnu/libredwg/libredwg-0.13.3.tar.xz -o libredwg.tar.xz
          tar xJf libredwg.tar.xz
          cd libredwg-0.13.3
          ./configure --disable-bindings \
                      --disable-write \
                      --enable-release \
                      --prefix=$HOME/libredwg-install
          make -j"$(nproc)"
          make install

      - name: Add LibreDWG to PATH
        run: |
          echo "$HOME/libredwg-install/bin" >> "$GITHUB_PATH"
          echo "LD_LIBRARY_PATH=$HOME/libredwg-install/lib:$LD_LIBRARY_PATH" >> "$GITHUB_ENV"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry & target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: Build fixture emitter
        run: cargo build -p aec_cad --example dwg_oracle_fixture --release

      - name: Emit per-version DWG fixtures
        run: |
          mkdir -p /tmp/dwg_oracle
          ./target/release/examples/dwg_oracle_fixture /tmp/dwg_oracle

      - name: Read each fixture via LibreDWG (dwgread)
        run: |
          set -e
          # Blocking gate. Each version must:
          #   - exit 0 from `dwgread`
          #   - emit no `Error:`/`ERROR` lines on stderr
          #   - emit no unfiltered `Warning:` lines (see allow-list)
          #
          # Allow-list group 1 (LibreDWG-decoder bugs — R2004+ only,
          # fire on LibreDWG's own bundled `example_2004.dwg` and
          # cannot be silenced from the writer side; both kept narrow
          # to a single, version-gated pair so a future writer
          # regression that produces a new warning still fails the
          # gate):
          #
          #   * "checksum: 0x........ (calculated) CRC mismatch ..." —
          #     `decode.c:3329` (marked `// FIXME`). LibreDWG hashes
          #     the live page-header checksum slot during validation
          #     instead of zeroing it, then compares to the same
          #     slot. No writer-side checksum value can satisfy the
          #     identity. LibreDWG's own encoder leaves it as the
          #     plain spec-correct Adler-32 over the zero-slot header.
          #
          #   * "Invalid sections: 0 != numgaps: 0 + numsections: ..."
          #     — `decode.c:1552`. LibreDWG reads `dwg->header.sections`
          #     from the legacy file header `PRE(R_2004a)` only
          #     (`header.spec:64`); on the R2004+ path the field stays
          #     at its default 0, so the equality check is statically
          #     false for any non-empty file. Cannot be fixed from the
          #     writer.
          #
          # Both warnings only apply to R2004+ versions (R2004 /
          # R2010 / R2013 / R2018); R12 / R14 / R2000 / R2007 use
          # different wire formats and must remain warning-free.
          ALLOW_R2004_PLUS='^Warning: checksum: 0x[0-9a-f]+ \(calculated\) CRC mismatch |^Warning: Invalid sections: 0 != numgaps: 0 \+ numsections: '

          # Allow-list group 2 (spec-correct HANDSEED dangling-handle
          # warning — modern-path versions only). Per the DWG spec
          # HANDSEED is "the next handle to allocate", i.e. one past
          # the highest in-use handle, and therefore by definition
          # not present in the object_map. LibreDWG's post-decode
          # `dwg_resolve_handle` loop (`dwg.c:907`) iterates every
          # `object_ref` — including the HANDSEED slot in header_vars
          # — and warns when a handle is missing. LibreDWG's own
          # bundled `example_2000.dwg` fires the same warning for its
          # `HANDSEED = 0xBE7`.
          #
          # The allow-list is intentionally scoped to the EXACT
          # decimal/hex pair our oracle fixture emits (36 / 0x24 —
          # one past the highest user-entity handle 0x23 in the
          # standard 3-entity fixture; see
          # `crates/aec_cad/examples/dwg_oracle_fixture.rs`). Any
          # OTHER dangling-handle regression — entity points at a
          # missing BLOCK_HEADER, corrupted owner pointer, typo in a
          # table record — produces a different numeric value (or
          # the long-form warning text from `dwg.c:900` when the
          # dangling handle is below HANDSEED), so the gate still
          # detects it.
          #
          # If the fixture entity count ever changes, update both
          # `dwg_oracle_fixture.rs` AND this pattern in lockstep.
          # The HANDSEED warning fires on every modern path
          # (R14 / R2000 / R2004 / R2007 / R2010 / R2013 / R2018);
          # R12 uses a different wire format (no header_vars
          # HANDSEED slot).
          ALLOW_HANDSEED='^Warning: Object handle not found 36/0x24$'
          fail=0
          for v in r12 r14 r2000 r2004 r2007 r2010 r2013 r2018; do
            path=/tmp/dwg_oracle/$v.dwg
            echo "::group::dwgread $v"
            log=$(mktemp)
            # Verbose level matters. `dwgread -O DXF -o file.dxf` at
            # default verbose suppresses all `Warning:` lines (only
            # `SUCCESS` / `ERROR:` reach stderr) — which would make
            # the allow-list filter below a vacuous no-op. `-v1`
            # surfaces post-decode warnings (Object handle not found,
            # CRC mismatch, etc.) so the allow-list actually
            # exercises and catches regressions. Every version is
            # run at `-v1` since PR-H1 lifted R2007's empty-doc
            # restriction — R2007 now ships the same BLOCK_CONTROL /
            # LAYER_CONTROL / model-space chain as R14+.
            if dwgread -v1 -O DXF -o /tmp/dwg_oracle/$v.dxf "$path" >"$log" 2>&1; then
              ec=0
            else
              ec=$?
            fi
            cat "$log"
            # Filter `Warning:` lines through the allow-list (R2004+
            # versions only); everything else must stay warning-free.
            case "$v" in
              r2004|r2010|r2013|r2018)
                # R2004+ allows both decoder-bug warnings and the
                # spec-correct HANDSEED warning.
                unexpected_warnings=$(grep "^Warning:" "$log" | grep -Ev "$ALLOW_R2004_PLUS" | grep -Ev "$ALLOW_HANDSEED" || true)
                ;;
              r14|r2000|r2007)
                # R14/R2000/R2007 allow the HANDSEED warning only;
                # the R2004+ decoder bugs don't apply (R14/R2000 use
                # a flat section-locator layout, R2007 uses the
                # decode_r2007 codepath which doesn't share the
                # buggy section_array / page-checksum slot logic).
                unexpected_warnings=$(grep "^Warning:" "$log" | grep -Ev "$ALLOW_HANDSEED" || true)
                ;;
              *)
                # R12 emits an empty-doc fixture with no header_vars
                # HANDSEED slot, so no warnings are expected.
                unexpected_warnings=$(grep "^Warning:" "$log" || true)
                ;;
            esac
            errors=$(grep -E "^Error:|^ERROR" "$log" || true)
            if [ "$ec" -ne 0 ]; then
              echo "::error::$v.dwg: dwgread exited $ec" >&2
              fail=$((fail + 1))
            fi
            if [ -n "$errors" ]; then
              echo "::error::$v.dwg: unexpected Error/ERROR lines" >&2
              echo "$errors" >&2
              fail=$((fail + 1))
            fi
            if [ -n "$unexpected_warnings" ]; then
              echo "::error::$v.dwg: unexpected Warning lines (outside allow-list)" >&2
              echo "$unexpected_warnings" >&2
              fail=$((fail + 1))
            fi
            if [ "$ec" -eq 0 ] && [ -z "$errors" ] && [ -z "$unexpected_warnings" ]; then
              echo "✓ $v.dwg parsed cleanly"
            fi
            rm -f "$log"
            echo "::endgroup::"
          done
          if [ "$fail" -ne 0 ]; then
            echo "LibreDWG oracle: $fail version(s) failed the gate."
            exit 1
          fi

      - name: Cross-check entity inventory in the emitted DXF
        run: |
          set -e
          # Each version that ships a non-empty document must contain
          # LINE, CIRCLE, and TEXT entities — the three the fixture
          # emitter writes. R12 (AC1009 wire format) is the only
          # remaining empty-document fixture and skips the inventory
          # check. PR-H1 lifted R2007's empty-doc restriction; it now
          # round-trips the same 3-entity geometry as R14+.
          # `dwgread` writes DOS line endings on its DXF output, so we
          # strip `\r` before matching.
          fail=0
          for v in r12 r14 r2000 r2004 r2007 r2010 r2013 r2018; do
            dxf=/tmp/dwg_oracle/$v.dxf
            if [ ! -f "$dxf" ]; then
              echo "::error::$v.dxf was not produced by dwgread" >&2
              fail=$((fail + 1))
              continue
            fi
            case "$v" in
              r12)
                echo "✓ $v: empty-document fixture (no entity inventory expected)"
                continue
                ;;
            esac
            stripped=$(tr -d '\r' < "$dxf")
            for kind in LINE CIRCLE TEXT; do
              if ! echo "$stripped" | grep -q "^${kind}$"; then
                echo "::error::$v.dxf missing $kind entity" >&2
                fail=$((fail + 1))
              fi
            done
            if [ "$fail" -eq 0 ]; then
              echo "✓ $v: DXF contains LINE / CIRCLE / TEXT"
            fi
          done
          if [ "$fail" -ne 0 ]; then
            echo "LibreDWG oracle: DXF inventory check failed."
            exit 1
          fi

      - name: Upload oracle artifacts for triage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: libredwg-oracle
          path: /tmp/dwg_oracle/
          retention-days: 7

  typescript:
    name: TypeScript (${{ matrix.os }})
    needs: gate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(needs.gate.outputs.os_matrix) }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npm run type-check

      - name: Test
        run: npm run test:ui

  # Phase 9 (PR4 + PR5) removed the `python-workers` job entirely
  # — both the Blender worker and the IFC worker (the only two
  # Python worker trees this repo ever shipped) have been replaced
  # with native Rust implementations. Rendering goes through
  # `crates/aec_render/src/{final_render,walkthrough,panorama}.rs`
  # and IFC I/O / tessellation through `crates/aec_bim/src/ifc/`
  # + `crates/aec_bim/src/tessellator.rs`. Adding a Python step
  # back here would defeat the point of the rewrite.

  # ---------------------------------------------------------------
  # package-*: produce installable artefacts.
  #
  # `needs: [rust, typescript]` gates packaging on a fully green
  # Rust matrix AND a fully green TypeScript matrix — a Rust
  # regression on `main` (e.g. a clippy failure on macOS-latest)
  # must not allow `electron-builder` to ship an installer that
  # links in a broken `aec_bridge` native addon.
  # ---------------------------------------------------------------
  package-linux:
    name: Package (Linux)
    runs-on: ubuntu-latest
    needs: [rust, typescript]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux runtime deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-0 libnss3 libxss1 libasound2 libnotify4

      # `npm run build` invokes the napi-rs build for the
      # `aec_bridge` native addon, which needs a Rust toolchain on
      # the packaging runner itself (the `rust` job runs on a
      # different machine, its toolchain is not shared). Without
      # this step the Linux installer would either fail at
      # `npm run build` or link in a stale prebuilt addon.
      # Matches the equivalent step in `package-macos` and
      # `package-windows`.
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build renderer + electron
        run: npm run build

      - name: Install electron-builder
        run: npm install --no-save electron-builder@^24.13.0

      - name: Build AppImage + deb
        working-directory: apps/desktop
        run: |
          npx electron-builder --linux AppImage deb \
            --config ../../packaging/linux/electron-builder.linux.yml

      - name: Upload Linux installers
        uses: actions/upload-artifact@v4
        with:
          name: linux-installers
          path: |
            apps/desktop/dist/installers/linux/*.AppImage
            apps/desktop/dist/installers/linux/*.deb

  package-macos:
    name: Package (macOS)
    runs-on: macos-latest
    needs: [rust, typescript]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build renderer + electron
        run: npm run build

      - name: Install electron-builder
        run: npm install --no-save electron-builder@^24.13.0

      - name: Build dmg + zip
        working-directory: apps/desktop
        env:
          # Skip notarization in CI; signing keys are populated only in
          # release jobs from secrets.
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: |
          npx electron-builder --mac dmg zip \
            --config ../../packaging/macos/electron-builder.macos.yml

      - name: Upload macOS installers
        uses: actions/upload-artifact@v4
        with:
          name: macos-installers
          path: |
            apps/desktop/dist/installers/macos/*.dmg
            apps/desktop/dist/installers/macos/*.zip

  package-windows:
    name: Package (Windows)
    runs-on: windows-latest
    needs: [rust, typescript]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build renderer + electron
        run: npm run build

      - name: Install electron-builder
        run: npm install --no-save electron-builder@^24.13.0

      - name: Build nsis + msi
        working-directory: apps/desktop
        run: |
          npx electron-builder --win nsis msi `
            --config ../../packaging/windows/electron-builder.windows.yml

      - name: Upload Windows installers
        uses: actions/upload-artifact@v4
        with:
          name: windows-installers
          path: |
            apps/desktop/dist/installers/windows/*.exe
            apps/desktop/dist/installers/windows/*.msi
